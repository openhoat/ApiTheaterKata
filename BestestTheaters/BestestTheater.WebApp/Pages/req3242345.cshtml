@page
@model BestestTheater.WebApp.Pages.req3242345Model
@{
}

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Date</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.BkngData_3s)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.Date.ToString("yyyy-MM-dd HH:mm") </td>
                <td>@item.Details</td>
            </tr>
        }
    </tbody>
</table>